const EXTRA_KEY = "ai-rpg-character-extra";
const STAT_NAMES = ["strength","constitution","agility","intelligence","perception","willpower","charisma","luck"];
const MAGIC_CATEGORIES = ["Fuoco","Acqua","Aria/Vento","Terra","Ghiaccio","Fulmine","Luce","Oscurità","Vita/Guarigione","Morte/Negromanzia","Natura","Metallo","Spazio","Tempo","Telecinesi","Illusione","Evocazione","Barriere/Protezione","Potenziamento","Maledizioni","Purificazione","Spirituale","Mentale","Dimensionale","Sangue","Arcana"];

const DEFAULT_EXTRA = {
    level: 1, summary: "", statistics: Object.fromEntries(STAT_NAMES.map(name => [name, 10])), abilities: [], skills: [],
    conditions: { health: 100, stamina: 100, mana: 0, status: "Normale" }, relationships: [],
    inventory: { money: { oro: 0, argento: 0, rame: 0 }, armor: [], weapons: [], items: [] },
    magic: Object.fromEntries(MAGIC_CATEGORIES.map(category => [category, []]))
};
let extra = loadExtra();
function clone(value){ return JSON.parse(JSON.stringify(value)); }
function cloneDefaultExtra(){ return clone(DEFAULT_EXTRA); }
function calculateLevel(statistics){ const values=STAT_NAMES.map(name=>Number(statistics?.[name])||0); const average=values.reduce((sum,value)=>sum+value,0)/values.length; return Math.max(1,Math.min(20,1+Math.floor(Math.max(0,average-25)/3.5))); }
function getCharacterFromStorage(){ try{const value=JSON.parse(localStorage.getItem("ai-rpg-character")); return value&&typeof value==="object"?value:null;}catch(_){return null;} }
function getFocus(statistics){ const labels={strength:"forza fisica",constitution:"resistenza",agility:"agilità",intelligence:"intelligenza",perception:"percezione",willpower:"volontà",charisma:"carisma",luck:"fortuna"}; const ranked=STAT_NAMES.map(name=>({name,value:Number(statistics?.[name])||0})).sort((a,b)=>b.value-a.value); return labels[ranked[0]?.name]||"versatilità"; }
function buildSummary(){ const character=getCharacterFromStorage(); if(!character)return "Genera un personaggio per ottenere un riepilogo completo."; const identity=character.identity||{}, psychology=character.psychology||{}, personality=character.personality||{}, race=identity.race||"razza sconosciuta", age=Number(identity.age)||0, focus=getFocus(extra.statistics); const personalityText=personality.personality_description||personality.traits||"Ha una personalità ancora da definire."; const desireText=psychology.desires?` Il suo principale obiettivo è ${psychology.desires}.`:""; const fearText=psychology.fears?` Tra le sue paure spicca ${psychology.fears}.`:""; return `${identity.name||"Questo personaggio"} è ${age>0?`un individuo di ${age} anni`:"un individuo"} di razza ${race}, con una predisposizione naturale verso la ${focus}. ${personalityText}${desireText}${fearText}`.replace(/\s+/g," ").trim(); }
function normalizeExtra(value){ const base=cloneDefaultExtra(); if(!value||typeof value!=="object")return base; const statistics={...base.statistics,...(value.statistics||{})}; const level=Number(value.level)>0?Number(value.level):calculateLevel(statistics); const inventory={...base.inventory,...(value.inventory||{})}; inventory.money={...base.inventory.money,...(inventory.money||{})}; inventory.armor=Array.isArray(inventory.armor)?inventory.armor:[]; inventory.weapons=Array.isArray(inventory.weapons)?inventory.weapons:[]; inventory.items=Array.isArray(inventory.items)?inventory.items:[]; const magic={...base.magic,...(value.magic||{})}; MAGIC_CATEGORIES.forEach(category=>{if(!Array.isArray(magic[category]))magic[category]=[];}); return {level:Math.max(1,Math.min(20,Math.round(level))),summary:typeof value.summary==="string"?value.summary:"",statistics,abilities:Array.isArray(value.abilities)?value.abilities:[],skills:Array.isArray(value.skills)?value.skills:[],conditions:{...base.conditions,...(value.conditions||{})},relationships:Array.isArray(value.relationships)?value.relationships:[],inventory,magic}; }
function refreshDerivedData(){extra.level=calculateLevel(extra.statistics); extra.summary=buildSummary();}
function loadExtra(){try{return normalizeExtra(JSON.parse(localStorage.getItem(EXTRA_KEY)));}catch(_){return cloneDefaultExtra();}}
function syncWithCharacterStorage(){try{const character=getCharacterFromStorage();if(!character)return;character.extra={...(character.extra||{}),...clone(extra)};localStorage.setItem("ai-rpg-character",JSON.stringify(character));}catch(_){} }
function saveExtra(){refreshDerivedData();localStorage.setItem(EXTRA_KEY,JSON.stringify(extra));syncWithCharacterStorage();renderSummary();}
function esc(value){return String(value??"").replace(/[&<>\"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[char]));}
function labelForStat(key){return {strength:"Forza",constitution:"Costituzione",agility:"Agilità",intelligence:"Intelligenza",perception:"Percezione",willpower:"Volontà",charisma:"Carisma",luck:"Fortuna"}[key]||key;}
function levelDescription(level){if(level<=3)return "Principiante";if(level<=6)return "Alle prime esperienze";if(level<=10)return "Competente";if(level<=14)return "Esperto";if(level<=17)return "Veterano";return "Eccezionale";}
function renderStatistics(){const root=document.getElementById("statisticsEditor");if(!root)return;root.innerHTML="";STAT_NAMES.forEach(key=>{const value=Number(extra.statistics[key])||0;root.insertAdjacentHTML("beforeend",`<div class="stat-row"><span>${labelForStat(key)}</span><input type="number" min="0" max="100" data-stat="${key}" value="${value}"></div>`);});root.querySelectorAll("[data-stat]").forEach(input=>input.addEventListener("input",()=>{extra.statistics[input.dataset.stat]=Math.max(0,Math.min(100,Number(input.value)||0));saveExtra();renderLevel();}));}
function renderLevel(){const level=Math.max(1,Math.round(extra.level||calculateLevel(extra.statistics)));const levelElement=document.getElementById("characterLevel"),textElement=document.getElementById("characterLevelText"),badge=document.getElementById("summaryLevelBadge");if(levelElement)levelElement.textContent=level;if(textElement)textElement.textContent=levelDescription(level);if(badge)badge.textContent=`LIV. ${level}`;}
function renderSummary(){const character=getCharacterFromStorage(),identity=character?.identity||{},psychology=character?.psychology||{},status=extra.conditions?.status||"Normale";const title=document.getElementById("summaryTitle"),text=document.getElementById("summaryText"),race=document.getElementById("summaryRace"),age=document.getElementById("summaryAge"),focus=document.getElementById("summaryFocus"),summaryStatus=document.getElementById("summaryStatus");if(!title)return;title.textContent=`${identity.name||"Nuovo"} ${identity.surname||"personaggio"}`.trim();text.textContent=extra.summary||buildSummary();race.textContent=identity.race||"—";age.textContent=Number(identity.age)>0?`${identity.age} anni`:"—";focus.textContent=getFocus(extra.statistics);summaryStatus.textContent=status||(psychology.mental_state?"Attivo":"Normale");renderLevel();}
function renderList(rootId,list,type){const root=document.getElementById(rootId);if(!root)return;root.innerHTML="";if(!list.length){root.innerHTML='<div class="extra-empty">Nessun elemento presente.</div>';return;}list.forEach((item,index)=>{const row=document.createElement("div");row.className="extra-item";row.innerHTML=`<div><strong>${esc(item.name)}</strong><span>${esc(item.description||"")}</span></div><button type="button" class="danger-button">Rimuovi</button>`;row.querySelector("button").addEventListener("click",()=>{extra[type].splice(index,1);saveExtra();renderList(rootId,extra[type],type);});root.appendChild(row);});}
function addListItem(type,nameId,descriptionId,rootId){const name=document.getElementById(nameId)?.value.trim(),description=document.getElementById(descriptionId)?.value.trim();if(!name)return;extra[type].push({name,description});document.getElementById(nameId).value="";document.getElementById(descriptionId).value="";saveExtra();renderList(rootId,extra[type],type);}
function renderConditions(){const map={conditionHealth:"health",conditionStamina:"stamina",conditionMana:"mana",conditionStatus:"status"};Object.entries(map).forEach(([id,key])=>{const input=document.getElementById(id);if(input)input.value=extra.conditions[key]??"";});}
function bindConditions(){const map={conditionHealth:"health",conditionStamina:"stamina",conditionMana:"mana",conditionStatus:"status"};Object.entries(map).forEach(([id,key])=>{const input=document.getElementById(id);if(!input)return;input.addEventListener("input",()=>{extra.conditions[key]=input.type==="number"?Math.max(0,Number(input.value)||0):input.value.trim();saveExtra();});});}
function renderMoney(){const root=document.getElementById("moneyEditor");if(!root)return;root.innerHTML="";Object.entries(extra.inventory.money).forEach(([currency,value])=>{const label=currency[0].toUpperCase()+currency.slice(1);root.insertAdjacentHTML("beforeend",`<div class="stat-row"><span>${label}</span><input type="number" min="0" value="${Number(value)||0}" data-money="${currency}"></div>`);});root.querySelectorAll("[data-money]").forEach(input=>input.addEventListener("input",()=>{extra.inventory.money[input.dataset.money]=Math.max(0,Number(input.value)||0);saveExtra();}));}
function renderInventory(){renderMoney();const armor=document.getElementById("armorEditor"),weapons=document.getElementById("weaponsEditor");if(armor){armor.innerHTML=extra.inventory.armor.length?extra.inventory.armor.map((item,index)=>`<div class="extra-item"><div><strong>${esc(item.name)}</strong><span>Condizioni: ${esc(item.condition??"—")} · Protezione: ${esc(item.protection??"—")} · ${esc(item.material||"")}</span></div><button type="button" class="danger-button" data-armor="${index}">Rimuovi</button></div>`).join(""):'<div class="extra-empty">Nessuna armatura equipaggiata.</div>';armor.querySelectorAll("[data-armor]").forEach(btn=>btn.addEventListener("click",()=>{extra.inventory.armor.splice(Number(btn.dataset.armor),1);saveExtra();renderInventory();}));}if(weapons){weapons.innerHTML=extra.inventory.weapons.length?extra.inventory.weapons.map((item,index)=>`<div class="extra-item"><div><strong>${esc(item.name)}</strong><span>${esc(item.description||"")}</span></div><button type="button" class="danger-button" data-weapon="${index}">Rimuovi</button></div>`).join(""):'<div class="extra-empty">Nessuna arma presente.</div>';weapons.querySelectorAll("[data-weapon]").forEach(btn=>btn.addEventListener("click",()=>{extra.inventory.weapons.splice(Number(btn.dataset.weapon),1);saveExtra();renderInventory();}));}renderList("inventoryItemsList",extra.inventory.items.map(item=>({name:`${item.name} ×${item.quantity||1}`,description:item.description})),"inventoryItems");}
function addInventoryItem(){const name=document.getElementById("inventoryItemName")?.value.trim();if(!name)return;const quantity=Math.max(1,Number(document.getElementById("inventoryItemQuantity")?.value)||1),description=document.getElementById("inventoryItemDescription")?.value.trim();extra.inventory.items.push({name,quantity,description});document.getElementById("inventoryItemName").value="";document.getElementById("inventoryItemQuantity").value=1;document.getElementById("inventoryItemDescription").value="";saveExtra();renderInventory();}
function renderMagic(){const root=document.getElementById("magicCategories");if(!root)return;root.innerHTML="";MAGIC_CATEGORIES.forEach(category=>{const card=document.createElement("div");card.className="card magic-category";const spells=extra.magic[category]||[];card.innerHTML=`<div class="card-title">${esc(category)}</div><div class="magic-list">${spells.length?spells.map((spell,index)=>`<div class="extra-item"><div><strong>${esc(spell.name)}</strong><span>${esc(spell.description||"")} · Padronanza: ${esc(spell.mastery??"—")}</span></div><button type="button" class="danger-button" data-spell="${index}">Rimuovi</button></div>`).join(""):'<div class="extra-empty">Nessuna magia conosciuta.</div>'}</div><div class="form-grid magic-add"><div class="field"><label>Nome magia</label><input data-magic-name="${esc(category)}"></div><div class="field"><label>Padronanza</label><input data-magic-mastery="${esc(category)}" placeholder="Principiante, esperto..."></div><div class="field field-wide"><label>Descrizione</label><input data-magic-description="${esc(category)}"></div></div><button type="button" class="secondary-button magic-add-button" data-magic-add="${esc(category)}">+ Aggiungi magia</button>`;root.appendChild(card);card.querySelectorAll("[data-spell]").forEach(btn=>btn.addEventListener("click",()=>{extra.magic[category].splice(Number(btn.dataset.spell),1);saveExtra();renderMagic();}));card.querySelector("[data-magic-add]").addEventListener("click",()=>{const name=card.querySelector("[data-magic-name]").value.trim();if(!name)return;const description=card.querySelector("[data-magic-description]").value.trim(),mastery=card.querySelector("[data-magic-mastery]").value.trim();extra.magic[category].push({name,description,mastery});saveExtra();renderMagic();});});}
function generateBaseExtra(){const race=(document.getElementById("race")?.value||"").toLowerCase();const bias={umano:{charisma:2,luck:2,intelligence:1},elfo:{agility:4,intelligence:5,perception:4,constitution:-2},nano:{strength:5,constitution:6,agility:-3},orco:{strength:7,constitution:5,intelligence:-4,charisma:-2},demone:{willpower:5,charisma:4},draconide:{strength:4,constitution:5,willpower:3,agility:-2},mezzelfo:{agility:3,intelligence:3,charisma:3}}[race]||{intelligence:1,charisma:1};const pool=[];for(let i=0;i<STAT_NAMES.length;i++)pool.push(Math.floor(Math.random()*41)+30);pool.sort(()=>Math.random()-.5);const statistics={};STAT_NAMES.forEach((key,index)=>{statistics[key]=Math.max(5,Math.min(95,pool[index]+(bias[key]||0)+Math.floor(Math.random()*13)-6));});const maxHealth=70+statistics.constitution*2+Math.floor(statistics.strength/2),maxStamina=50+statistics.constitution+statistics.agility,maxMana=20+statistics.intelligence+statistics.willpower;extra={...cloneDefaultExtra(),statistics,conditions:{health:maxHealth,stamina:maxStamina,mana:maxMana,status:"Normale"}};saveExtra();renderAll();}
function setExtra(value){extra=normalizeExtra(value);refreshDerivedData();saveExtra();renderAll();}
function getExtra(){refreshDerivedData();return clone(extra);}
function resetExtra(){extra=cloneDefaultExtra();saveExtra();renderAll();}
function renderAll(){renderStatistics();renderList("abilitiesList",extra.abilities,"abilities");renderList("skillsList",extra.skills,"skills");renderList("relationshipsList",extra.relationships,"relationships");renderConditions();renderInventory();renderMagic();renderSummary();}
function initCharacterSections(){["statistics","abilities","skills","conditions","inventory","magic","relationships"].forEach(section=>document.querySelector(`.nav-button[data-section="${section}"]`)?.classList.remove("disabled"));document.getElementById("addAbilityButton")?.addEventListener("click",()=>addListItem("abilities","abilityName","abilityDescription","abilitiesList"));document.getElementById("addSkillButton")?.addEventListener("click",()=>addListItem("skills","skillName","skillDescription","skillsList"));document.getElementById("addRelationshipButton")?.addEventListener("click",()=>addListItem("relationships","relationshipName","relationshipDescription","relationshipsList"));document.getElementById("addInventoryItemButton")?.addEventListener("click",addInventoryItem);document.getElementById("generateBaseStatsButton")?.addEventListener("click",generateBaseExtra);document.getElementById("resetExtraButton")?.addEventListener("click",resetExtra);bindConditions();renderAll();}
document.addEventListener("DOMContentLoaded",initCharacterSections);
window.aiRpgCharacterSections={reset:resetExtra,generateBase:generateBaseExtra,render:renderAll,getData:getExtra,setData:setExtra};
